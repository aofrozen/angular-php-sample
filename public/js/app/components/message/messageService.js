angular.module('socialSample.messageService', []).factory('messageService', [function () {
    var messageService = {};
    messageService._getFriendsList = function () {

    };

    messageService._updateFriendsList = function () {

    };

    messageService._getContactsList = function () {

    };

    messageService._getChats = function () {

    };

    messageService._load = function () {
        /*
         download contacts, chats, buddy list from Firebase
         */
    };

    return messageService;
}]);